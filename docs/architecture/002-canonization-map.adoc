= Pricing Canonization Map

[cols="1,3"]
|===
| Canon | Pricing mapping

| Canon000 | Subject vocabulary is `Price*`; Symfony bootstrap `Kernel` and `PricingBundle` are framework exceptions.
| Canon007 | `App\\Pricing\\ => src/` is literal PSR-4 identity.
| Canon009 | No Host implementation namespace dependency is introduced.
| Canon018 | Composer identity is `pricing/price`, mapping component token to `App\\Pricing\\` and subject token to `Price*`.
| Canon020 | No generic Domain, Core, Support, Runtime, Infrastructure, Utility, Helper, Port, Adapter, or Adaptor roots are created.
| Canon022 | Standalone runtime directly requires Objecting, Cruding, Collectioning, Tabling, Viewing, Interfacing, and EasyAdmin.
| Canon023 | Local first-party repositories use Composer path repositories with `symlink: true`.
| Canon024 | `composer.prod.json` contains no sibling path repositories or symlink directives.
| Canon025 | `bin/console`, `config/bundles.php`, `src/Kernel.php`, and `src/PricingBundle.php` provide dual runtime mode.
| Canon026 | PHP is `^8.4`; Symfony runtime packages are `^8.1`.
| Canon029 | Repository-owned PHP-CS-Fixer and PHPStan dependencies, configs, and scripts exist.
| Canon031 | Production classes contain meaningful English PHPDoc descriptions.
| Canon032 | `PricingBundle` is registered in standalone `config/bundles.php`.
| Canon033 | Development and production manifests share package/type/PSR-4/PHP/Symfony identity.
| Canon034 | `.gitignore` covers dependency, runtime, local environment, quality cache, IDE, OS, and Node noise.
| Canon039 | PHPUnit dependency/config/scripts include `src/`, branch coverage, and persistent text summary.
| Canon043 | Local sibling dependencies are pinned to exact `dev-master` with path repository version identity.
|===

