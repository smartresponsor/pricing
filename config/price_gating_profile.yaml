component:
  name: Pricing
  nameEntity: pricing
  namespace: App\Pricing
  subject_prefix: Price
  business_prefix: Price
  database_prefix: pricing_
  typed_layers_required: true
  service_interface_mirror: true

  route_owner_root: price
  route_owner_roots:
    - price
  mutation_mode: forbidden

  secret_scan: enabled
  docblock_preservation: enabled
  release:
    evidence_required: false

